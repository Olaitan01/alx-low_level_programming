#include "main.h"
/**
 * main - returns a negative or  positive
 * if integer is either negative or positive
 *
 * Return: Always 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
