#include "main.h"

/**
 * add - returns the sum of the parameters
 *
 * @c: The value to be passed as parameter
 * @b: The value to be passed as parameter
 * Return: Always 0
 */
int add(int c, int b)
{
	int result = c + b;

	return (result);
}
